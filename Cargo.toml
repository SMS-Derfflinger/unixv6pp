[package]
name = "rust_kernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_kernel"
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = 2
